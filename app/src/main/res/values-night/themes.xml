<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ViewResources" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/theme_dark_primary</item>
    </style>

    <style name="AltButtonStyle" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/theme_dark_secondary</item>
    </style>

    <style name="Theme.ViewResources" parent="Base.Theme.ViewResources" />
</resources>